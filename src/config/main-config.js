// When dotenv is configured, it will look for .env in the root and load environment variables into memory.
require("dotenv").config();

// Configuration object that will configure all middleware:
module.exports = {
  init() {}
};
